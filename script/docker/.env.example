## ============================================
## 芋道系统 Docker 部署环境变量模板
## 使用方法：复制本文件为 .env 并填写实际值
##   cp .env.example .env
## 所有敏感值请替换为强密码（建议 16 位以上，含大小写字母、数字、特殊字符）
## ============================================

## ---------- MySQL ----------
MYSQL_ROOT_PASSWORD=请修改为强密码

## ---------- Redis ----------
REDIS_PASSWORD=请修改为强密码

## ---------- PostgreSQL（RAG 向量数据库） ----------
POSTGRES_PASSWORD=请修改为强密码

## ---------- Grafana（监控看板） ----------
GRAFANA_PASSWORD=请修改为强密码

## ---------- 应用数据源（通常与 MYSQL_ROOT_PASSWORD 一致）----------
MASTER_DATASOURCE_PASSWORD=请修改为强密码
SLAVE_DATASOURCE_PASSWORD=请修改为强密码

## ---------- MyBatis 加解密秘钥 ----------
## 生产环境必须修改，影响数据库字段加解密
MYBATIS_ENCRYPTOR_PASSWORD=请修改为强密码

## ---------- API 加密秘钥（开启 api-encrypt 时必须配置）----------
## 必须为 16、24 或 32 位字符串
API_ENCRYPT_REQUEST_KEY=请修改为16位以上密钥
API_ENCRYPT_RESPONSE_KEY=请修改为16位以上密钥

## ---------- Alertmanager 告警通知 ----------
## 钉钉/企业微信 webhook URL（如不需要告警通知可保留默认值）
# DINGTALK_WEBHOOK_URL=https://oapi.dingtalk.com/robot/send?access_token=YOUR_TOKEN
