server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://zhicloud-loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: zhicloud-server
    static_configs:
      - targets:
          - localhost
        labels:
          job: zhicloud-server
          __path__: /var/log/zhicloud-server/*.log
    pipeline_stages:
      # 提取 traceId（logback Pattern 中的 [%X{traceId:-}]）
      - regex:
          expression: '\[(?P<traceId>[a-f0-9-]+)\] \[(?P<tenantId>[^\]]*)\]'
      - labels:
          traceId:
          tenantId:
